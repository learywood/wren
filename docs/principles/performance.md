# Performance

Wren must be lightning quick. Startup should feel instant, and normal operation must be performant by default without requiring user tuning.

Performance profiling is a key project metric. Profiling and performance measurements must provide continuous backpressure against regressions and unnecessary overhead.
