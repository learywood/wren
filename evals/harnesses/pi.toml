schema_version = 1
harness = "pi"
executable = "pi.cmd"
profile = "default-local-oauth"
provider = "openai-codex"
model = "gpt-5.6-sol"
reasoning = "low"
tools = ["read", "write"]
authentication = "local"
